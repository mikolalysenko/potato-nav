#include <cstdint>

#include "csr.h"
#include "generate.h"

using namespace SPUD;
using namespace Generate;

CSRGraph* random(int64_t numVerts, int64_t numArcs) {

}

CSRGraph* ring(int64_t numVerts) {
  auto result = CSRGraph
}

CSRGraph* hub(int64_t numVerts) {
}
